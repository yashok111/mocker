# UI review, 2026-09-05 — six readers over the whole admin UI

Six independent reads after `A20` emptied the `EXEMPT` map: three Opus
agents and three `vcodex` (`gpt-5.6-luna`, effort high), one pair per angle —
(1) contract vs screen, (2) forms and edit paths, (3) navigation and first
run. The vcodex read of angle 1 burned its window spawning sub-audits and
returned no table; it was relaunched with a hard budget (its findings, if
any, are appended at the end). The raw reports are session artifacts; this
file is the deduplicated, ranked record — the count in «readers» is how
many of the five independent reports named the same thing.

Already fixed the same day, before this file (each with a test): the drift
repairs had no confirm (2 readers); the stream edit form's PUT omitted
`listSize`/`delayMs`/`reqSchema`; the transfer panel's copy promised
scenarios in the bundle (the export carries none; the fork does); the
stats strip read the admin feed's registry instead of the mock plane's; the
header's status word came off stale query data; a 401 on either probe
bounced the session.

## Bugs — wrong behaviour or data loss (build first)

**All closed in `A21` step 1 (2026-09-05), one test each**, except B8's
traffic half: the badge says «опрос каждые 2 с» while `connecting` because
the poll IS the feed until the stream is live (the screen's own state
machine, `TrafficPage.tsx`), so the copy is true and stays. B9 came from
the sixth reader (below). The variant fixes (B2, B4, B5) landed as the
narrowest change in each form, not the `VariantEditor` — that refactor
stays step 5.

| # | readers | where | what | fix |
|---|---|---|---|---|
| B1 | 3 | `ConnectPanel.tsx`, `connect/recipes.ts` | The four copy-paste recipes and the shown address are `workspace.url`, which by contract carries NO `basePath`; a workspace with `basePath: /api/v1` hands the operator an address every request 404s on, and nothing on the panel names basePath. The guide claims the panel shows it. | Append `settings.basePath` to the address and every recipe; one line explaining it. |
| B2 | 1 | `CustomEndpointsPage.tsx` edit form, non-function branch | A P7a variant `{mode: generated, schema}` has an empty body box; editing only the path resends it as `mode: pinned, body: undefined` — a pinned empty body, and the schema stops serving. `ContractPage` deep-links into exactly this form. | Keep the stored mode when the body box is empty and the variant has `schema`; show «тело строится по схеме» instead of an empty box. |
| B3 | 1 | `SettingsPanel.tsx` conflict reload | After a 409 the form is re-seeded from the conflict's `details`, but `onSubmit` still builds the wire object from the stale `workspace.settings` prop — the retry resends pre-conflict `basePath`, `cors`, `notFoundBody`, `identity`. | Keep `details.settings` in state and pass it as the base to `buildSettings`. |
| B4 | 1 | `OperationEditor.tsx` | `Variant.function` is legal on a spec-operation override too (A18: "both writers"), but A20 put the Lua box only on the custom-endpoint forms; a function-bearing override renders as «сгенерированный» with «Тело строится по схеме спеки» — false. | Port A20's box and badge into the per-status variant editor. |
| B5 | 1 | `OperationEditor.tsx`, `CustomEndpointsPage.tsx` | A `bodyRef` variant shows `{}` in the body box (`variant.body ?? {}`); the first keystroke adds `body` beside `bodyRef` → 400 naming a field the form never rendered. | Show «ответ — файл <name>» read-only and disable the body box until the ref is cleared (see G4). |
| B6 | 2 | `StreamEditor.tsx` caps strip | `StreamPreviewView.nominalRate` and `StreamPreviewFrame.notRun` are not rendered: a Lua tick's SAMPLE rate is labelled «Максимум» and a not-run frame prints `null`. | «оценка по одному запуску» when `nominalRate`; «тело не вычислялось» for `notRun`. |
| B7 | 1 | `ResourcesPage.tsx` | With no spec bound, two alerts contradict each other («не привязана спека…» then «в привязанной спеке не нашлось…»). | Make the empty-list branch subordinate to `specId !== null`; link the first alert to the overview. |
| B8 | 1 | `TrafficPage.tsx`, `ResourcesPage.tsx` | Stale copy: the transport badge says «опрос каждые 2 с» while still `connecting`; the resources intro says paging is «следующий слайс» after A20 shipped it. | The resources string; the badge is correct (see above). |
| B9 | 1 | `WorkspacesPage.tsx` | A 2xx the contract does not declare rendered as «у вас пока нет воркспейсов» instead of the error state every other screen shows. | The standard error branch. |

## Gaps — a field or a tool with no screen (ranked by readers, then value)

**G1 and G2 closed in `A21` step 4 (2026-09-05)**: a «Маршрут и доступ»
section on the settings panel — `basePath`, the values box that appears
when it carries a `{param}`, the CORS mode and credentials switch, the 404
body as JSON — plus `identity.id` (org stays the agent's: it is an object,
preserved by the spread); and «что внутри» on a scenario row, a viewer
over `GET .../scenarios/{sid}` listing the overridden operations and the
settings the snapshot carries (the editor stays refused, per `CARVE-OUTS.md`).

**G3, G7, G8, G9, G10, G12 and U8 closed in `A21` step 3 (2026-09-05)**:
slug and spec on the create card with `?specId=` from the specs screen's
«Создать воркспейс с этой спекой» and an «открыть» link on the created
line; checkpoint id and «с данными ресурсов» on the row plus the rollback's
own result line (`dataRestored`); any forced status, «сколько запросов»
(`n`), «снять» per directive and `setAt`; the two switches on the
custom-endpoint edit form; the import summary as a notification; a confirm
on a RE-bind that points at «Проверить спеку»; «Пресет авторизации»,
«Сессия», «Что подставляем», «правки схемы», «автоматических значений» in
place of the wire words.

| # | readers | where | what the operator cannot see or do | proposal | effort |
|---|---|---|---|---|---|
| G1 | 4 | `SettingsPanel.tsx` | `basePath`, `basePathValues`, `cors`, `notFoundBody` (and `identity.id/org`) are preserved and never shown; `update_workspace_settings` writes all of them. A `{param}` basePath with no values declared serves nothing, and the screen cannot say why. | A «Маршрут и доступ» section: basePath, the values list when it carries `{param}`, the two CORS knobs, the 404 body. | M |
| G2 | 4 | `ScenariosPage.tsx` | A row shows name, date, «активен» — never what the snapshot holds; `GET .../scenarios/{sid}` is called only by the mask banner AFTER activation. | Expand a row into the same list the mask banner renders (not an editor — `CARVE-OUTS.md` refuses the editor, not a viewer). | M |
| G3 | 3 | `WorkspacesPage.tsx`, `SpecsPage.tsx` | Creation sends only `name` (`slug`, `specId` dropped; the slug is permanent); after a spec import there is no «создать воркспейс с этой спекой». | Slug + spec select on the create card (the import modal already has both); a CTA on the import result and on each spec row. | S |
| G4 | 2 | variant editors | `Variant.bodyRef` has no field; the «Файлы» tab prints the syntax as prose. | «Ответ — файл» selector over `GET .../assets`, exclusive with body/mediaType. | M |
| G5 | 2 | variant editors | `Variant.headers` — `Location`, `Retry-After`, `X-Request-Id` — settable by the agent, invisible on screen. | A key/value list next to «Media type», both screens. | M |
| G6 | 3 | `ResourceEntities.tsx`, `ResourcesPage.tsx` | Rows can be edited and deleted, never CREATED (PUT is create-by-key); `byBaseScope` counts and the `scopeKey`/`baseScopeKey` filters are unused, so a parameterised basePath shows one aggregate count. | «Добавить запись» with key + scope fields; per-base-scope counts on the row; a scope filter on the list. | M |
| G7 | 3 | `HistoryPage.tsx` | Checkpoint id and `hasData` are not on the row (the rollback label cites an id no row shows); `RollbackResponseView.dataRestored` is never read, so «вернуть и данные» that restored nothing says nothing. | Id and a «с данными» badge on the row; a result line after rollback. | S |
| G8 | 2 | `SessionControls.tsx` | Forced status is hardcoded 503, «сломать следующий» is always `once` (no `n`), the only removal is «Очистить всё» though the clear route takes one `{target, action}`; `setAt` is not shown. | Status input, «сколько запросов», a ✕ per directive row, relative `setAt`. | S |
| G9 | 2 | `CustomEndpointsPage.tsx` edit form | `routeOff`/`overrideOn` are a badge with no control; disabling a custom endpoint means deleting it. | The same two switches the operation editor has. | S |
| G10 | 2 | `WorkspacesPage.tsx` import | `ImportWorkspaceView.specCreated`/`entitiesRestored` are discarded on navigate. | A notification or banner on the landed workspace. | S |
| G11 | 1 | `OperationEditor.tsx` | No way to delete ONE status variant (only «Сбросить к спеке»), no way to add a status the spec does not declare, and the preview cannot send headers or a body though `when[]` can gate on both. | «Убрать этот статус», a «+» tab, headers/body inputs on the preview. | M |
| G12 | 1 | `SettingsPanel.tsx` spec select | Re-binding a spec PATCHes on change with no confirm and no pointer at «Проверить спеку». | Confirm modal; on success point at the drift check. | S |
| G13 | 1 | `AssetsPage.tsx` | Upload under an existing name replaces silently (2 readers); `sha256` never shown. | «файл будет заменён» before the button; a truncated digest per row. | S |
| G14 | 1 | scattered | `reset-data` lives on «История», not «Ресурсы»; `?all=1` and `ownerId` never used (a colleague's workspace is invisible); `routing`/`baseDomain` never shown on the connect panel; `limits.maxEntities` and the two retentions shown nowhere; `forkedFrom` never shown. | One line or one toggle each. | S each |
| G15 | 1 | contract | `OpOverrideResponseSummary` has `mode`, `recipeCount`, `hasSchemaPatch` but no `hasFunction`, so the operations LIST cannot badge a function even after B4. | Add `hasFunction` server-side; badge it. | S |

## Usability — the same product, easier to hold in the head

**U1–U4 closed in `A21` step 2 (2026-09-05)**: `WorkspaceContextBar.tsx`
under the name on every tab (spec by name linking to the overview, the
active scenario as a badge linking to «Сценарии», «директив сессии: N»
linking to «Endpoint'ы», «ревизия N» with a tooltip; each query degrades
on its own); «Воркспейсы» in the header and a `WorkspaceSwitcher` on
workspace routes; `TabLink.tsx` and the seven prose references turned into
links; the traffic «Совпадение» cell links by `?opId=` (new search param
on the operations route, resolved through the spec operations list) or
`?endpointId=`, and both «в переопределение»/«в endpoint» toasts carry
«Открыть».

| # | readers | what | proposal | effort |
|---|---|---|---|---|
| U1 | 5 | The four pieces of live state that decide what the mock answers — the bound spec BY NAME (today «спека #3»), the active scenario, the number of session directives in force, and «ревизия N» with no gloss — live on four different tabs. Every reader named this. | A context bar in `WorkspaceLayout` above the tabs: spec name+version, «сценарий: X» linking to the tab, «директив: N», revision with a tooltip. | M |
| U2 | 2 | No «Воркспейсы» in the header, no switcher; the wordmark is the only way back. | A nav item plus a `Select` switcher when a workspace route is active. | S |
| U3 | 3 | Seven «на вкладке X» phrases in prose, one real link. | `Anchor`s everywhere, the pattern `CustomEndpointsPage.tsx` already uses. | S |
| U4 | 3 | A traffic row shows `operation #12` as text; «в переопределение»/«в endpoint» end in a toast. The deep links `?opKey=`/`?endpointId=` already work. | Link the match cell; an «Открыть» action on the toast. | S |
| U5 | 2 | The traffic empty state is one bare sentence at the moment the person asks «дошёл ли запрос?»; no filter/search on the screen the guide sends people to first. | URL + the health curl in the empty state; client-side method/status/path filters. | S / M |
| U6 | 2 | «Endpoint'ы» routes to `/operations`, «Кастомные» to `/endpoints`; ten tabs mix the core flow with files/connections/contract. | Rename «Операции спеки» / «Свои эндпоинты» (or a caption on the operations screen); regroup the tab order. | S |
| U7 | 2 | The overview stacks five panels and the spec binding — the one thing a fresh workspace needs — is the last divider of the third. | With `specId === null`, a «Привязать спеку» card first; otherwise a status line on top. | S |
| U8 | 4 | Wire words and English on screen: «Auth preset», «Session», «Media type», «Рецепт», `schemaPatch`, `routeOff`. §14 forbids the first class; the guide uses different names than the screen. | Rename to the guide's words; «Правки схемы применены», «Операция выключена». | S |
| U9 | 1 | Switching operations remounts the editor and drops unsaved changes silently. | A dirty flag, a note by «Сохранить», a confirm before the parent switches. | S |
| U10 | 1 each | «Повторить» with no «К списку» on a 404 workspace; the created-slug line is not a link (import navigates, create does not); login fields say nothing about get-or-create and the shared password, and `/guide` is behind the guard; «раздел «Функции» в руководстве» is prose though `/guide#…` anchors exist; no copy button on the family URL; `?opKey=` is a one-shot seed, not the URL; per-frame JSON errors only at the top and no frame reorder; `reset-data` sends an empty slug where rollback checks locally; asset MIME/size not checked before PUT. | One small change each. | S each |

## Three structural readings

- **One `VariantEditor`** (all three Opus reads, independently). The variant
  — `{mode, body, bodyRef, function, schema, schemaPatch, recipes, headers,
  when[], mediaType, bodyEncoding}` — is the product's unit of authoring and
  is rendered by two divergent partial forms, each showing four or five of
  the eleven fields; that is why A18 reached one form and not the other,
  and why `headers` reached neither. One component over the whole schema,
  with the exclusivity rules (function ⊻ body ⊻ bodyRef) as unreachable
  states, mounted by both screens, closes B2, B4, B5, G4, G5, G9 and G11 at
  once and turns "does the screen know the new field" into a type error.
- **A dashboard overview with a context bar** (vcodex 3, Opus 3). Spec,
  scenario, directives, drift, traffic in one strip, one primary action
  («Подключить фронтенд» / «Привязать спеку» / «Проверить спеку»), settings
  and transfer below; the same strip reachable from every tab.
- **Read → change → preflight → write** (vcodex 2). Every editor exposes the
  whole document it replaces, marks preserved-but-uneditable fields, and
  attaches server refusals to fields. The same idea as the first, said for
  settings, streams and resources too.

## Deliberate absences the readers met (not gaps)

Recipe editing (`CARVE-OUTS.md` "P2 UI debt"), the schema/`operation`
editor on custom endpoints (P7b), an HTTP custom-endpoint preview (A18 D7),
the endpoint filter on «Соединения» (P6e), `validateRequests` (unread by
design), `confidence` (a constant). Two partial reopens worth pricing, both
S: a «правки схемы: N» badge on a variant that has a `schemaPatch` (the
display half only), and `operation.summary`/`tags` as two text inputs so a
UI-created endpoint lands in the «Контракт» tab named.

## The sixth reader (vcodex, angle 1, relaunched with a hard budget)

Returned 18 rows, 15 of them already above (G1, G3, G4, G5, G6, G8, G9,
G11, G13, G14, U-items). New: B9 (taken); `TrafficRow.truncated` is only
implicit through disabled tooltips — a persistent «обрезано» badge beside
the bodies (S, joins G14); `StreamConnectionView.endpointId` not shown on
a connection row (S, joins G14); `createdAt`/`updatedAt` on the workspace
list and header (S, joins G14). Rejected: `ResourceSuggestion.confidence`
is a written constant (`CARVE-OUTS.md`).

## Suggested order for `A21`

1. B1–B8 (the bugs), one commit — an afternoon.
2. U1 + U2 + U3 + U4 (the context bar and the links) — the change every
   reader asked for, one day.
3. G3 + G7 + G8 + G9 + G10 + G12 + U8 (small gaps and copy) — one day.
4. G1 (settings section) and G2 (scenario viewer) — one day.
5. The `VariantEditor` refactor carrying G4, G5, G11, B4, B5 — two days,
   and the only item that needs a design pass first.
