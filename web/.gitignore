node_modules/
dist/
.vite/
coverage/

# Yarn 4 (corepack-managed, node-modules linker)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*

# Generated by the TanStack Router plugin / `tsr generate`
src/routeTree.gen.ts

# Generated by orval from ../api/openapi.json
src/api/generated/

# Scratch directory the TanStack Router plugin creates during a dev/build run.
# Empty between runs, so git never reported it — `git clean -nd` did.
.tanstack/
